# ============================================================
#  Local deploy config  —  copy this to .deploy.env and fill in
#  DO NOT commit .deploy.env to git (it's in .gitignore)
#
#  NOTE: env files for backend, frontend, and admin already
#  exist on the EC2 server — no need to set them here.
# ============================================================

# EC2 connection
EC2_HOST=210.79.129.163
EC2_USER=ubuntu

# Absolute path to your EC2 SSH private key file
EC2_SSH_KEY_PATH=/Users/apple/.ssh/turborepo-excloud-ssh
