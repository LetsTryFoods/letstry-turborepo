import { MongoClient } from 'mongodb';

async function run() {
  const uri = "mongodb://localhost:27017/letstryfoods"; // Guessing local DB URI or whatever EXPO_PUBLIC_GRAPHQL_URL connects to. Wait, what DB does backend use?
}
